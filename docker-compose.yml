version: "3.9"
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000"
    depends_on:
      - backend
  backend:
    build:
      context: ./explorations
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
